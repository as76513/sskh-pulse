node_modules/
dist/
.env
.DS_Store
*.log
.npm/

# stray asset copy — the real one lives at frontend/src/assets/logo.jpg
/Shubh-Shree-logo.jpg

# Terraform
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/*.tfplan
infra/terraform.tfvars
infra/.terraform.lock.hcl
infra/backend.hcl
infra/build/

# bootstrap/ keeps its own local state on purpose (see infra/README.md) —
# back it up outside of git, don't rely on committing it.
infra/bootstrap/.terraform/
infra/bootstrap/*.tfstate
infra/bootstrap/*.tfstate.*
infra/bootstrap/*.tfplan
infra/bootstrap/.terraform.lock.hcl
